/** @type {import('next').NextConfig} */
const nextConfig = {
  // Cho phép kết nối tới backend API với self-signed certificate (dev)
  // Tailwind CSS đã được cấu hình sẵn qua postcss
};

export default nextConfig;
